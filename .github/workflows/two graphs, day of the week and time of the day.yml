import pandas as pd
import matplotlib.pyplot as plt
from datetime import datetime

# Assuming you have a DataFrame named 'df' with a column named 'Time Posted' containing timestamps, and columns 'Reach' and 'Likes'

# Convert 'Time Posted' column to datetime
df['Time Posted'] = pd.to_datetime(df['Time Posted'])

# Extract day of the week and time of the day
df['Day of Week'] = df['Time Posted'].dt.day_name()
df['Time of Day'] = df['Time Posted'].dt.strftime('%H:%M')

# Plotting Day of Week versus Reach and Likes
fig, axes = plt.subplots(nrows=1, ncols=2, figsize=(12, 4))

# Plot for Reach
axes[0].bar(df['Day of Week'], df['Reach'])
axes[0].set_title('Day of Week vs Reach')
axes[0].set_xlabel('Day of Week')
axes[0].set_ylabel('Reach')

# Plot for Likes
axes[1].bar(df['Day of Week'], df['Likes'])
axes[1].set_title('Day of Week vs Likes')
axes[1].set_xlabel('Day of Week')
axes[1].set_ylabel('Likes')

plt.tight_layout()
plt.show()

# Plotting Time of Day versus Reach and Likes
fig, axes = plt.subplots(nrows=1, ncols=2, figsize=(12, 4))

# Plot for Reach
axes[0].plot(df['Time of Day'], df['Reach'])
axes[0].set_title('Time of Day vs Reach')
axes[0].set_xlabel('Time of Day')
axes[0].set_ylabel('Reach')

# Plot for Likes
axes[1].plot(df['Time of Day'], df['Likes'])
axes[1].set_title('Time of Day vs Likes')
axes[1].set_xlabel('Time of Day')
axes[1].set_ylabel('Likes')

# Set y-axis interval to 1 hour
axes[0].yaxis.set_major_locator(ticker.MultipleLocator(base=1))
axes[1].yaxis.set_major_locator(ticker.MultipleLocator(base=1))

plt.tight_layout()
plt.show()
